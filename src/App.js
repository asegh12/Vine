import React from "react";
import { Reset } from "styled-reset";
import Template from "./components/Template";

const App = () => {
  return (
    <React.Fragment>
      <Reset />
      <Template />
    </React.Fragment>
  );
};

export default App;
